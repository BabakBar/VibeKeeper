# OpenAI API Key (LiteLLM will auto-detect this)
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Specify model (defaults to gpt-4o-mini)
LITELLM_MODEL=gpt-4o-mini

# ---------------------------------------------------------------------------
# Backend configuration
# ---------------------------------------------------------------------------

# JWT signing key (generate with `openssl rand -hex 32` for real deployments)
JWT_SECRET_KEY=your-jwt-secret-key-here

# Database URL (leave default for local SQLite)
# DATABASE_URL=sqlite+aiosqlite:///./occasions.db